'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // Todas as tabelas serão criadas automaticamente pelo Sequelize
    // Migrations manuais podem ser adicionadas aqui se necessário
  },

  async down(queryInterface, Sequelize) {
    // Reverter migrações se necessário
  }
};